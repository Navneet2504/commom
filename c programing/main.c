#include<stdio.h>
#include<math.h>
void prime(int n)
{
    for(int i)
}
    void oddeven(int n)
    {
        if (n % 2 == 0)
        {
            printf("even");
        }
        else
        {
            printf("odd");
        }
    }

int main()
{
    int n;
    printf("enter the no.");
    scanf("%d",&n);
    prime(n);
    oddeven(n);
    return 0;
}